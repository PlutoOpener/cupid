/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "media.licdn.com",
      "img.icons8.com",
      "www.flaticon.com",
      "upload.wikimedia.org",
    ],
  },
};

export default nextConfig;
